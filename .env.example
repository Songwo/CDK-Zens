HCAPTCHA_SECRET=0x0000000000000000000000000000000000000000
HCAPTCHA_VERIFY_URL=https://api.hcaptcha.com/siteverify
HCAPTCHA_SITE_KEY=10000000-ffff-ffff-ffff-000000000001
VITE_HCAPTCHA_SITE_KEY=10000000-ffff-ffff-ffff-000000000001

# MySQL 配置
# Linux 服务器部署使用 host 网络，容器内的 127.0.0.1 就是服务器本机。
# 写在 .env 里时，密码里的 $ 保持单个 $；只有直接写进 docker-compose.yml 才需要写成 $$。
CDK_MYSQL_DSN=Zens:CHANGE_ME@tcp(127.0.0.1:3306)/cdk_airdrop?parseTime=true&charset=utf8mb4&loc=Local

# 可选增强组件。默认留空，应用可直接使用 MySQL 运行。
# 如需启用：docker compose --profile cache up -d --build
CDK_AIRDROP_REDIS_URL=
CDK_AIRDROP_RABBITMQ_URL=

# 社区 SSO 配置
CDK_COMMUNITY_URL=http://localhost:5173
CDK_COMMUNITY_CLIENT_ID=cdk-airdrop
CDK_COMMUNITY_JWT_SECRET=your_jwt_secret_here
